server.port=8080
spring.datasource.url=jdbc:postgresql://localhost:5432/medfis_db
spring.datasource.username=medfis_user
spring.datasource.password=medfis_pass
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false
jwt.secret=MedFis2024SecretKeyParaJWTMustBe256BitsLongAtLeast32Chars!
jwt.expiration=86400000
cors.allowed-origins=http://localhost:5173,http://localhost:3000
logging.level.com.medfis=DEBUG

# ── Serialización de fechas como ISO-8601 (no como array) ─────────────────────
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.write-date-timestamps-as-nanoseconds=false
spring.jackson.time-zone=America/Bogota

# ── CORREO Gmail SMTP ──────────────────────────────────────────────────────────
# IMPORTANTE: usar App Password de Google, NO la contraseña normal
# myaccount.google.com → Seguridad → Verificación en 2 pasos (activar)
# → Contraseñas de aplicación → "Correo" → "Windows/Mac" → Generar (16 chars)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MEDFIS_MAIL_USER:vemaurijames@gmail.com}
spring.mail.password=${MEDFIS_MAIL_PASSWORD:jckaqktrasmtyyre}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000

# Datos del remitente
medfis.mail.from=${MEDFIS_MAIL_USER:vemaurijames@gmail.com}
medfis.mail.from-name=CliniSign - Consentimientos Informados
medfis.mail.copy-to=${MEDFIS_MAIL_COPY:vemaurijames@gmail.com}
medfis.whatsapp.clinica=${MEDFIS_WA_CLINICA:573114048112}

# ── WhatsApp Twilio — automático ──────────────────────────────────────────────
# Obtener credenciales en: https://console.twilio.com
# Sandbox gratuito: número +1 415 523 8886 — el destinatario debe enviar "join <palabra>" una vez
# Producción: solicitar número WhatsApp Business en Twilio Console → Messaging → WhatsApp
twilio.account-sid=${TWILIO_ACCOUNT_SID:REEMPLAZAR}
twilio.auth-token=${TWILIO_AUTH_TOKEN:REEMPLAZAR}
twilio.wa-from=${TWILIO_WA_FROM:whatsapp:+14155238886}

# Número WhatsApp del administrador (recibe solicitudes de acceso desde el login)
medfis.wa.admin=${MEDFIS_WA_ADMIN:573114048112}

# Nombre de la IPS para los mensajes de WhatsApp
medfis.nombre-ips=${MEDFIS_NOMBRE_IPS:CliniSign}
